# Conformance tests

Language-neutral fixtures and expected canonical outputs belong here. Fixtures
must document provenance and may not be copied from LeanCTX or Graft.

The `v1/manifest.json` file maps each original synthetic fixture to its target
schema and expected verdict. Every invalid fixture should isolate one primary
rejection class so failures remain diagnosable.
