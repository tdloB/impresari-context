[package]
name = "context-retrieval"
description = "Bounded deterministic retrieval and context packaging"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
base64 = { version = "=0.23.1", default-features = false, features = ["std"] }
context-core = { version = "=0.2.0", path = "../context-core" }
context-store = { version = "=0.2.0", path = "../context-store" }
context-workspace = { version = "=0.2.0", path = "../context-workspace" }
sha2 = { version = "=0.11.0", default-features = false }
