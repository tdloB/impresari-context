[package]
name = "context-reference-client"
description = "Independent non-OS programmatic reference consumer"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
context-core = { version = "=0.2.0", path = "../context-core" }
context-engine = { version = "=0.2.0", path = "../context-engine" }

[dev-dependencies]
context-store = { version = "=0.2.0", path = "../context-store" }
context-workspace = { version = "=0.2.0", path = "../context-workspace" }

